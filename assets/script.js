const { constants, hiscores } = require("osrs-api");
let gimpNames = ["MyroTheGimp", "Homage49", "Chickenbobv0", "GIMofAnarchy", "gimpy4u"]

//loop through array and get stats

for (let i = 0; i < gimpNames.length; i++) {
    const element = gimpNames[i];
    
}

//